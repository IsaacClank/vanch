{
  "name": "vanch-event-broker-adapter",
  "version": "0.0.1-alpha.1",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist node_modules",
    "test": "jest --silent",
    "test.watch": "jest --watchAll --silent --verbose",
    "test.changes": "jest --watch"
  },
  "dependencies": {
    "ioredis": "^5.2.4",
    "typescript": "^4.9.4"
  },
  "devDependencies": {
    "@types/jest": "^29.2.4",
    "http-server": "^14.1.1",
    "jest": "^29.3.1",
    "lerna": "^6.1.0",
    "ts-jest": "^29.0.3"
  },
  "workspaces": [
    "packages/*"
  ]
}
